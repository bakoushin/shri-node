html
  head
    title Git Eye
  body
    h2= branch
    include _branches.pug
    include _tabs.pug
    include _tree.pug
  p
    a(href='/' + branch + '/files/' + path + '/..') ..
  img(src='/' + src)
