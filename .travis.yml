sudo: required
services:
  - docker
language: node_js
node_js:
  - "node"
install:
  - npm install
cache:
  directories:
  - "node_modules"
git:
  depth: false
script:
  - npm run lint
  - npm run test
  - npm run prod
after_success:
  - docker image build --tag bakoushin/git-eye:1.0 .